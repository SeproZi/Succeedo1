rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to app data only if the user is authenticated
    match /{path=**}/documents {
       allow read, write: if request.auth != null;
    }
  }
}
